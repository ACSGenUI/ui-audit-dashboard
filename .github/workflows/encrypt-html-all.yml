name: Encrypt HTML (all)

on:
  workflow_dispatch:

jobs:
  encrypt-all:
    permissions:
      contents: write
    uses: ./.github/workflows/encrypt-reports-reusable.yml
    with:
      mode: all
      commit_msg: 'chore: encrypt all HTML from main'
